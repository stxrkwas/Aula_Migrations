# Notas

- Caso a versão do composer for incompatível com a versão do php, adicionar o comando '"platform-check": false' como último argumento no trecho '"config"' do arquivo 'composer.json'.
- Em seguida, digitar o comando 'composer dump-autoload' no terminal e apertar enter.